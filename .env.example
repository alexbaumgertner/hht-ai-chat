MONGODB_URI=mongodb://127.0.0.1:27017/hht-ai-chat

# Optional: set to enable real AI-generated responses via the Vercel AI SDK.
# When unset, the app replies in a deterministic local (mock) mode.
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
